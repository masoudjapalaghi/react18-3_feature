const ActPage = () => {
  return (
    <div>
      lets you defer loading component’s code until it is rendered for the first time.
      <a href="https://react.dev/reference/react/lazy">Documenting</a>
    </div>
  );
};

export default ActPage;
